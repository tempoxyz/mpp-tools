[project]
name = "mpp-conformance-adapter-python"
version = "0.1.0"
description = "Python conformance adapter for MPP tooling"
license = { text = "MIT" }
requires-python = ">=3.12"
dependencies = [
    "pympp[tempo]==0.8.2",
]
